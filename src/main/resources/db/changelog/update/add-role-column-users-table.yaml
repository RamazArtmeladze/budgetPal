databaseChangeLog:
  - changeSet:
      id: 2-add-role-column-to-users-table
      author: ramaz
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: role
                  type: varchar(20)
                  defaultValue: USER
                  constraints:
                    nullable: false